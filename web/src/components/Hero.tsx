export default function Hero() {
  return (
    <header className="rounded-lg border border-border bg-card p-5 shadow-card">
      <p className="text-xs uppercase tracking-[0.16em] text-accent">Idea Foundry Studio</p>
      <h1 className="mt-2 text-3xl">Turn rough notes into a traceable planning dossier</h1>
      <p className="mt-2 max-w-3xl text-sm text-foreground/80">
        Paste messy fragments, apply constraints, and generate a structured product brief with lineage, source traces,
        and a viability signal in one pass.
      </p>
    </header>
  );
}
